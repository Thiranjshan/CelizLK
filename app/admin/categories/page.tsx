'use client';

import { FormEvent, useEffect, useState } from 'react';
import { LayoutGrid, Pencil, Plus, Trash2, X } from 'lucide-react';
import AdminAuthGate from '@/components/AdminAuthGate';

interface Attribute { attributeKey: string; attributeLabel: string; dataType: string; unit: string | null; }
interface Category { id: string; name: string; slug: string; description: string | null; attributes: Attribute[]; _count: { products: number }; }
interface DraftAttribute { key: string; label: string; dataType: string; unit: string; }

export default function CategoriesPage() { return <AdminAuthGate active="categories">{(_, token) => <CategoriesManager token={token} />}</AdminAuthGate>; }

function CategoriesManager({ token }: { token: string }) {
  const [categories, setCategories] = useState<Category[]>([]);
  const [form, setForm] = useState({ name: '', slug: '', description: '' });
  const [attributes, setAttributes] = useState<DraftAttribute[]>([]);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [error, setError] = useState(''); const [message, setMessage] = useState('');
  async function load() { const response = await fetch('/api/admin/categories', { headers: { Authorization: `Bearer ${token}` } }); const result = await response.json(); if (!response.ok) throw new Error(result.error); setCategories(result); }
  useEffect(() => { fetch('/api/admin/categories', { headers: { Authorization: `Bearer ${token}` } }).then(async (response) => { const result = await response.json(); if (!response.ok) throw new Error(result.error); setCategories(result); }).catch((reason) => setError(reason.message)); }, [token]);
  function reset() { setEditingId(null); setForm({ name: '', slug: '', description: '' }); setAttributes([]); }
  function edit(category: Category) { setEditingId(category.id); setForm({ name: category.name, slug: category.slug, description: category.description || '' }); setAttributes(category.attributes.map((attribute) => ({ key: attribute.attributeKey, label: attribute.attributeLabel, dataType: attribute.dataType, unit: attribute.unit || '' }))); window.scrollTo({ top: 0, behavior: 'smooth' }); }
  async function save(event: FormEvent) { event.preventDefault(); setError(''); const response = await fetch(editingId ? `/api/admin/categories/${editingId}` : '/api/admin/categories', { method: editingId ? 'PATCH' : 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify({ ...form, attributes }) }); const result = await response.json(); if (!response.ok) { setError(result.error); return; } setMessage(editingId ? 'Category updated.' : 'Category created.'); reset(); await load(); }
  async function remove(category: Category) { if (category._count.products > 0 || !window.confirm(`Delete ${category.name}?`)) return; const response = await fetch(`/api/admin/categories/${category.id}`, { method: 'DELETE', headers: { Authorization: `Bearer ${token}` } }); if (!response.ok) { const result = await response.json(); setError(result.error); return; } setMessage('Category deleted.'); await load(); }
  return <><header className="admin-header"><div><div className="admin-kicker">Catalog structure</div><h1>Categories</h1><p>Manage categories and the specification attributes used by products.</p></div><LayoutGrid size={30} color="var(--accent-purple)" /></header>{error && <div className="admin-error" role="alert">{error}</div>}{message && <div className="admin-success">{message}</div>}<form className="admin-form-panel" onSubmit={save}><div className="admin-panel-heading"><div><h2>{editingId ? 'Edit category' : 'Create category'}</h2><p>Attributes become dynamic fields in the product form.</p></div>{editingId && <button type="button" className="admin-icon-button" onClick={reset}><X size={18} /></button>}</div><div className="admin-form-grid"><label>Name<input required minLength={2} value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} /></label><label>Slug<input disabled={Boolean(editingId)} value={form.slug} placeholder="Generated from name" onChange={(event) => setForm({ ...form, slug: event.target.value })} /></label><label className="admin-form-wide">Description<textarea rows={3} value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} /></label></div>{!editingId && <div className="admin-form-section"><h3>Specification attributes</h3>{attributes.map((attribute, index) => <div className="admin-attribute-row" key={`${attribute.key}-${index}`}><input required placeholder="Key" value={attribute.key} onChange={(event) => setAttributes(attributes.map((item, itemIndex) => itemIndex === index ? { ...item, key: event.target.value } : item))} /><input required placeholder="Label" value={attribute.label} onChange={(event) => setAttributes(attributes.map((item, itemIndex) => itemIndex === index ? { ...item, label: event.target.value } : item))} /><select value={attribute.dataType} onChange={(event) => setAttributes(attributes.map((item, itemIndex) => itemIndex === index ? { ...item, dataType: event.target.value } : item))}><option value="text">Text</option><option value="number">Number</option><option value="boolean">Boolean</option></select><input placeholder="Unit" value={attribute.unit} onChange={(event) => setAttributes(attributes.map((item, itemIndex) => itemIndex === index ? { ...item, unit: event.target.value } : item))} /><button type="button" className="admin-icon-button" onClick={() => setAttributes(attributes.filter((_, itemIndex) => itemIndex !== index))}><X size={16} /></button></div>)}<button type="button" className="admin-outline-button" onClick={() => setAttributes([...attributes, { key: '', label: '', dataType: 'text', unit: '' }])}><Plus size={16} /> Add attribute</button></div>}<button className="btn-primary">{editingId ? 'Save category changes' : 'Create category'}</button></form><section className="admin-panel admin-wide-panel"><div className="admin-panel-heading"><div><h2>Current categories</h2><p>{categories.length} categories in the database</p></div></div>{categories.length ? <div className="admin-category-grid">{categories.map((category) => <article className="admin-category-card" key={category.id}><div><h3>{category.name}</h3><small>/{category.slug} · {category._count.products} products</small></div><p>{category.description || 'No description'}</p><div className="admin-attribute-list">{category.attributes.length ? category.attributes.map((attribute) => <span key={attribute.attributeKey}>{attribute.attributeLabel}</span>) : <em>No attributes</em>}</div><div className="admin-row-actions"><button onClick={() => edit(category)} title="Edit category"><Pencil size={15} /> Edit</button><button className="admin-danger-button" onClick={() => remove(category)} disabled={category._count.products > 0}><Trash2 size={15} /> {category._count.products ? 'Has products' : 'Delete'}</button></div></article>)}</div> : <div className="admin-empty">No categories found.</div>}</section></>;
}