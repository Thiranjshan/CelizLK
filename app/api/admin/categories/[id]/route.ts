import { NextResponse } from 'next/server';
import { getAdminFromRequest, hasAdminPermission, writeAudit } from '@/lib/admin-auth';
import { prisma } from '@/lib/prisma';

export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const admin = await getAdminFromRequest(request); if (!admin) return NextResponse.json({ error: 'Not authenticated.' }, { status: 401 }); if (!hasAdminPermission(admin.role, ['PRODUCT_MANAGER'])) return NextResponse.json({ error: 'Forbidden.' }, { status: 403 }); const id = (await params).id; const body = await request.json(); const data: { name?: string; description?: string | null; seoTitle?: string | null; seoDescription?: string | null } = {}; if (typeof body.name === 'string' && body.name.trim().length >= 2) data.name = body.name.trim().slice(0, 80); if (body.description === null || typeof body.description === 'string') data.description = body.description?.slice(0, 500) || null; if (body.seoTitle === null || typeof body.seoTitle === 'string') data.seoTitle = body.seoTitle?.slice(0, 160) || null; if (body.seoDescription === null || typeof body.seoDescription === 'string') data.seoDescription = body.seoDescription?.slice(0, 320) || null; const attributes = Array.isArray(body.attributes) ? body.attributes.filter((attribute: { key?: unknown; label?: unknown; dataType?: unknown }) => typeof attribute.key === 'string' && typeof attribute.label === 'string' && ['text', 'number', 'boolean'].includes(String(attribute.dataType || 'text'))).slice(0, 30) : null; if (!Object.keys(data).length && attributes === null) return NextResponse.json({ error: 'No valid changes supplied.' }, { status: 400 }); const category = await prisma.$transaction(async (transaction) => { const updated = await transaction.category.update({ where: { id }, data }); if (attributes !== null) { await transaction.categoryAttribute.deleteMany({ where: { categoryId: id } }); await transaction.categoryAttribute.createMany({ data: attributes.map((attribute: { key: string; label: string; dataType?: string; unit?: string }) => ({ categoryId: id, attributeKey: attribute.key.slice(0, 60), attributeLabel: attribute.label.slice(0, 100), dataType: attribute.dataType || 'text', unit: attribute.unit?.slice(0, 20) })) }); } return updated; }); await writeAudit(admin.id, 'UPDATE', 'CATEGORY', id, { ...data, attributes: attributes?.length }); return NextResponse.json(category); }
export async function DELETE(request: Request, { params }: { params: Promise<{ id: string }> }) { const admin = await getAdminFromRequest(request); if (!admin) return NextResponse.json({ error: 'Not authenticated.' }, { status: 401 }); if (!hasAdminPermission(admin.role, ['PRODUCT_MANAGER'])) return NextResponse.json({ error: 'Forbidden.' }, { status: 403 }); const id = (await params).id; const count = await prisma.product.count({ where: { categoryId: id } }); if (count) return NextResponse.json({ error: 'Category still contains products.' }, { status: 409 }); await prisma.category.delete({ where: { id } }); await writeAudit(admin.id, 'DELETE', 'CATEGORY', id, {}); return NextResponse.json({ success: true }); }