import { NextResponse } from 'next/server';
import { getAdminFromRequest, hasAdminPermission, writeAudit } from '@/lib/admin-auth';
import { prisma } from '@/lib/prisma';

export async function GET(request: Request, { params }: { params: Promise<{ key: string }> }) { const admin = await getAdminFromRequest(request); if (!admin) return NextResponse.json({ error: 'Not authenticated.' }, { status: 401 }); if (!hasAdminPermission(admin.role, ['MARKETING'])) return NextResponse.json({ error: 'Forbidden.' }, { status: 403 }); const key = (await params).key; return NextResponse.json(await prisma.siteContent.findUnique({ where: { key } })); }
export async function PATCH(request: Request, { params }: { params: Promise<{ key: string }> }) { const admin = await getAdminFromRequest(request); if (!admin) return NextResponse.json({ error: 'Not authenticated.' }, { status: 401 }); if (!hasAdminPermission(admin.role, ['MARKETING'])) return NextResponse.json({ error: 'Forbidden.' }, { status: 403 }); const key = (await params).key; const body = await request.json(); const value = JSON.stringify(body.value); if (key.length > 100 || value.length > 20000) return NextResponse.json({ error: 'Content is too large.' }, { status: 400 }); const content = await prisma.siteContent.upsert({ where: { key }, update: { value }, create: { key, value } }); await writeAudit(admin.id, 'UPDATE', 'SITE_CONTENT', key, { value }); return NextResponse.json(content); }