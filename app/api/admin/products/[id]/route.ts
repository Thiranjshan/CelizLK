import { NextResponse } from 'next/server';
import { getAdminFromRequest, hasAdminPermission, writeAudit } from '@/lib/admin-auth';
import { prisma } from '@/lib/prisma';

export async function GET(request: Request, { params }: { params: Promise<{ id: string }> }) { const admin = await getAdminFromRequest(request); if (!admin) return NextResponse.json({ error: 'Not authenticated.' }, { status: 401 }); if (!hasAdminPermission(admin.role, ['PRODUCT_MANAGER'])) return NextResponse.json({ error: 'Forbidden.' }, { status: 403 }); const product = await prisma.product.findUnique({ where: { id: (await params).id }, include: { category: true, brandRecord: true, inventoryLogs: { orderBy: { createdAt: 'desc' }, take: 100 } } }); if (!product) return NextResponse.json({ error: 'Product not found.' }, { status: 404 }); return NextResponse.json({ ...product, images: JSON.parse(product.images), specs: product.specs }); }
export async function DELETE(request: Request, { params }: { params: Promise<{ id: string }> }) { const admin = await getAdminFromRequest(request); if (!admin) return NextResponse.json({ error: 'Not authenticated.' }, { status: 401 }); if (!hasAdminPermission(admin.role, ['PRODUCT_MANAGER'])) return NextResponse.json({ error: 'Forbidden.' }, { status: 403 }); const id = (await params).id; const product = await prisma.product.update({ where: { id }, data: { isActive: false, status: 'ARCHIVED' } }); await writeAudit(admin.id, 'ARCHIVE', 'PRODUCT', id, {}); return NextResponse.json(product); }